import { GetDateFromStringPipe } from './get-date-from-string.pipe';

describe('GetDateFromStringPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDateFromStringPipe();
    expect(pipe).toBeTruthy();
  });
});
