import { ConvertConvertedCurrencyPipe } from './convert-converted-currency.pipe';

describe('ConvertConvertedCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertConvertedCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
