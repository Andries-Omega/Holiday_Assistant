import { GetListOfDatesPipe } from './get-list-of-dates.pipe';

describe('GetListOfDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new GetListOfDatesPipe();
    expect(pipe).toBeTruthy();
  });
});
