import { ListOfCurrenciesToArrPipe } from './list-of-currencies-to-arr.pipe';

describe('ListOfCurrenciesToArrPipe', () => {
  it('create an instance', () => {
    const pipe = new ListOfCurrenciesToArrPipe();
    expect(pipe).toBeTruthy();
  });
});
